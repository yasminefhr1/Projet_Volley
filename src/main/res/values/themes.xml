<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base theme for activities with custom toolbar (NoActionBar) -->
    <style name="Base.Theme.Projetws" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Theme for activities with default action bar (like AddEtudiant) -->
    <style name="Theme.Projetws.DefaultActionBar" parent="Theme.Material3.DayNight">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
    </style>

    <style name="Theme.Projetws" parent="Base.Theme.Projetws" />
</resources>